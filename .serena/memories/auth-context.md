AuthContext provides centralized auth state: user (current logged-in), loading, isAdmin (role==='admin'), isPending (status==='pending'), isSuspended, canInteract. Methods: login, logout, signup, updateProfile, updateLastActive (tracks active session). Phase 4: Session-based auth (HttpOnly cookie + session table), PHP helpers/auth.php (validateSession, getCurrentUser), credentials:'include' in fetch(). Phase 5: Migrate from localStorage to API authentication calls.